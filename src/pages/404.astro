---
import Base from '../layouts/Base.astro';
import { SITE_TITLE } from '../consts';
---

<Base title={`Not found · ${SITE_TITLE}`} description="Page not found">
  <h1>404 — not found</h1>
  <p>That page doesn't exist. Try the <a href="/blog">blog index</a>.</p>
</Base>
